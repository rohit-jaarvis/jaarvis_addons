<odoo>
    <data>
        <!--here we will add current user to the group group_multi_company-->
        <!--so that the employee module will show company_id field-->
        <record model="res.groups" id="base.group_multi_company">
            <field name="users" eval="[(4, ref('base.user_root'))]"/>
        </record>

    </data>
</odoo>